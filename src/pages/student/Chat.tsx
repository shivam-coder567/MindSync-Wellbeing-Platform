import { Paperclip, Send, Video } from "lucide-react";
import { useState } from "react";
export default function Chat() { const [message, setMessage] = useState(""); const [messages, setMessages] = useState([{ mine: false, text: "Hi Demo, I’m glad you reached out. How has your week been feeling?", time: "10:04 AM" }, { mine: true, text: "A little overwhelming, but I’m trying to take it one day at a time.", time: "10:06 AM" }, { mine: false, text: "That sounds like a thoughtful way to meet it. What has helped you feel even a little steadier?", time: "10:07 AM" }]); function send() { if (!message.trim()) return; setMessages((items) => [...items, { mine: true, text: message.trim(), time: "Now" }]); setMessage(""); }
  return <main className="page"><p className="eyebrow">Your support team</p><h1>Messages</h1><p className="lead">A private space to stay in touch with your support professional.</p><section className="two-column" style={{ marginTop: 30 }}><div className="surface chat-panel"><div className="chat-header"><div style={{ display: "flex", alignItems: "center", gap: 12 }}><div className="avatar" style={{ width: 42, height: 42, background: "#dfeedd" }}>DP</div><div><h2 style={{ marginBottom: 3 }}>Dr. Demo Professional</h2><span className="online">● Typically replies within a day</span></div></div><button className="btn btn-outline" aria-label="Start video consultation"><Video size={17} /></button></div><div className="messages" aria-live="polite">{messages.map((item, index) => <div key={index} className={`message ${item.mine ? "mine" : ""}`}><span className="message-label">{item.mine ? "You" : "Dr. Demo Professional"} · {item.time}</span>{item.text}</div>)}</div><div className="composer"><button className="btn btn-outline" aria-label="Attach a file"><Paperclip size={17} /></button><input aria-label="Message your professional" placeholder="Write a message..." value={message} onChange={(event) => setMessage(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") send(); }} /><button className="btn btn-primary" onClick={send}>Send <Send size={15} /></button></div></div><aside className="surface side-tip"><h3>Conversation note</h3><p style={{ color: "#70827a", fontSize: 13, lineHeight: 1.55 }}>Messages are a great place for reflections between sessions. If something feels urgent, please use immediate support instead of waiting for a reply.</p><div className="info-box" style={{ marginTop: 22 }}>Your last session: <strong style={{ color: "#36544a" }}>Aug 14</strong><br />Next availability: <strong style={{ color: "#36544a" }}>This week</strong></div></aside></section></main>; }
